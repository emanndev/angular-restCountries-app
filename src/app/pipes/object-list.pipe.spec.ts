import { ObjectListPipe } from './object-list.pipe';

describe('ObjectListPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectListPipe();
    expect(pipe).toBeTruthy();
  });
});
